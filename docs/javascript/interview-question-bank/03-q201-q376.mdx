---
title: Q201–Q376
description: JavaScript interview questions 201–376 with answers, reasoning, misconceptions, follow-ups, and chapter mapping.
id: 03-q201-q376
---

# Q201–Q376
export const topics = [{"n":201,"category":"thenables","answer":"Promise resolution assimilates thenables by retrieving/calling a `then` method under defined rules, so arbitrary thenables can trigger code and edge cases.","wrong":"Only native Promise instances participate in promise chaining.","related":"chapter 32"},{"n":205,"category":"Promise combinators","answer":"`all`, `allSettled`, `any`, and `race` encode different completion/failure policies; choose by business semantics rather than habit.","wrong":"`Promise.all` runs functions in parallel by itself.","related":"chapter 32"},{"n":209,"category":"async function return","answer":"An async function always returns a Promise; returning a value fulfills it and throwing rejects it. `await` is syntax over promise-based suspension/resumption.","wrong":"`async` makes a function run on another thread.","related":"chapter 33"},{"n":213,"category":"sequential vs concurrent await","answer":"Starting independent promises before awaiting them can overlap work; awaiting each operation before starting the next creates a waterfall.","wrong":"Using `await` anywhere automatically makes all operations concurrent.","related":"chapter 33"},{"n":217,"category":"ECMAScript Jobs vs browser tasks","answer":"Promise reactions are ECMAScript Jobs; browsers integrate them with microtask processing inside a host event loop, while timers, I/O tasks, and rendering are host-defined.","wrong":"The ECMAScript specification defines `setTimeout` and the browser rendering loop.","related":"chapter 34"},{"n":221,"category":"microtask starvation","answer":"Continuously queueing microtasks can delay browser task processing/rendering because hosts typically drain microtasks at checkpoints before moving on.","wrong":"Microtasks always yield to rendering after each callback.","related":"chapter 34"},{"n":225,"category":"async iteration","answer":"`Symbol.asyncIterator`/`for await...of` support values arriving asynchronously; consumers should reason about backpressure and cancellation/closing.","wrong":"`for await...of` just turns a normal loop into a worker thread.","related":"chapter 35"},{"n":229,"category":"ESM live bindings","answer":"Imports observe the exporting module's binding, not a copied snapshot; import bindings themselves are read-only to the importer.","wrong":"An import is just a `const` initialized with the exported value.","related":"chapter 36"},{"n":233,"category":"module cycles","answer":"ESM separates linking/instantiation from evaluation, so cycles can be valid, but accessing an imported lexical binding before initialization can fail.","wrong":"Circular imports are always syntax errors.","related":"chapter 36"},{"n":237,"category":"explicit resource management","answer":"`using`/`await using` and disposal symbols provide deterministic cleanup semantics; as of July 2026 this is Stage 4 post-ES2026 living-standard work, so compatibility must still be checked.","wrong":"`using` is part of ES2026 and universally available.","related":"chapter 37"},{"n":241,"category":"Proxy invariants","answer":"Proxy traps can intercept operations but cannot violate invariants required by the target's non-configurable/non-extensible state.","wrong":"A Proxy trap can return any result regardless of target descriptors.","related":"chapter 38"},{"n":245,"category":"Reflect","answer":"Reflect provides function-form counterparts for fundamental operations and pairs naturally with Proxy forwarding, often returning booleans instead of using assignment syntax.","wrong":"Reflect bypasses normal object semantics and ignores Proxies.","related":"chapter 39"},{"n":249,"category":"WeakRef/FinalizationRegistry","answer":"Weak references/finalizers are nondeterministic GC-related facilities; correctness and resource cleanup must not depend on when callbacks happen.","wrong":"FinalizationRegistry is a deterministic destructor mechanism.","related":"chapter 41"},{"n":253,"category":"reachability and leaks","answer":"Objects remain collectible based on reachability, not whether you conceptually 'need' them; listeners, timers, caches, closures, and detached DOM can retain graphs.","wrong":"JavaScript memory leaks only happen when the engine GC is broken.","related":"chapter 42"},{"n":257,"category":"destructuring evaluation","answer":"Destructuring is binding/assignment syntax with defaults, nesting, rest, and iterator/property-access semantics; defaults run only for `undefined`.","wrong":"Destructuring deep-clones the source.","related":"chapter 43"},{"n":261,"category":"object spread","answer":"Object spread copies enumerable own properties into a new object using defined property-copy semantics; it is shallow and differs from prototype/descriptor cloning.","wrong":"`{...obj}` creates an exact clone including prototype and descriptors.","related":"chapter 44"},{"n":265,"category":"nullish coalescing","answer":"`??` falls back only for `null`/`undefined`, unlike `||` which falls back for any falsy value.","wrong":"`??` and `||` differ only in syntax preference.","related":"chapter 45"},{"n":269,"category":"JSON vs JavaScript","answer":"JSON is a data syntax with a narrower value model than JavaScript; stringify/parse have replacer/reviver hooks and defined omissions/errors such as BigInt handling.","wrong":"Any JavaScript expression is valid JSON.","related":"chapter 46"},{"n":273,"category":"Intl","answer":"ECMA-402 defines internationalization APIs separately from core ECMA-262; locale negotiation, calendars, numbering systems, time zones, collation, segmentation and formatting belong here.","wrong":"Intl behavior is part of the ECMA-262 core language algorithms.","related":"chapter 47"},{"n":277,"category":"event delegation","answer":"Browser event bubbling lets a stable ancestor handle events for dynamic descendants; `target` and `currentTarget` answer different questions.","wrong":"Event delegation means every descendant has its own listener behind the scenes.","related":"chapter 54"},{"n":281,"category":"fetch failure model","answer":"Browser `fetch` rejects for network/abort-style failures, not simply because HTTP status is 4xx/5xx; callers must inspect response status/ok.","wrong":"A 404 causes `fetch()` to reject automatically.","related":"chapter 56"},{"n":285,"category":"CORS boundary","answer":"CORS is a browser policy controlling cross-origin response access; it is not server-side authentication or authorization.","wrong":"Enabling CORS makes an API public and bypasses authentication.","related":"chapter 56"},{"n":289,"category":"browser storage","answer":"localStorage/sessionStorage/IndexedDB are host storage mechanisms with different sync/async, quota, serialization, and security properties; secrets should not be assumed safe simply because stored client-side.","wrong":"localStorage is a secure place for long-lived secrets because other websites cannot read it.","related":"chapter 58"},{"n":293,"category":"Web Workers","answer":"Workers provide separate execution agents for browser JavaScript and communicate by messaging/structured clone or shared memory; they help CPU-bound main-thread work but have overhead.","wrong":"A Web Worker shares the same DOM and can update elements directly.","related":"chapter 60"},{"n":297,"category":"XSS sinks","answer":"XSS risk arises when attacker-controlled data reaches executable HTML/script/navigation contexts without appropriate handling; prefer safe DOM APIs and strong policy controls.","wrong":"Escaping once with a generic function makes data safe for every browser context.","related":"chapter 62"},{"n":301,"category":"execution contexts/environments","answer":"ECMA-262 uses execution contexts and environment records to model running code and bindings; these concepts explain scope, globals, functions, modules and `this` without prescribing engine memory layout.","wrong":"Lexical environments are V8 heap objects mandated by the specification.","related":"chapter 63"},{"n":305,"category":"Reference Records and method calls","answer":"Identifier/property evaluation can produce reference-like specification records that carry base/receiver information; method-call syntax preserves a receiver while extracting the function loses it.","wrong":"`this` is stored permanently inside every normal function object.","related":"chapter 65"},{"n":309,"category":"abstract operations","answer":"Operations like ToPrimitive, Get, Set, Call, Construct and ToString are specification building blocks used by many language features; learn to trace them, not memorize every step.","wrong":"Abstract operations are JavaScript functions available at runtime.","related":"chapter 66"},{"n":313,"category":"Realms","answer":"A Realm has its own global object/intrinsics, so values from iframes/other realms can fail `instanceof` checks against constructors from the current realm.","wrong":"There is only one `Array` constructor identity in an entire browser process.","related":"chapter 67"},{"n":317,"category":"early errors","answer":"Some invalid programs are rejected during parsing/static semantic checks before execution, distinct from runtime exceptions.","wrong":"Every JavaScript error can be caught with a surrounding runtime try/catch.","related":"chapter 69"},{"n":321,"category":"JIT mental model","answer":"Engines may interpret, profile, optimize, deoptimize, use inline caches/shapes, and GC, but these are implementation strategies rather than ECMAScript guarantees.","wrong":"JavaScript semantics require V8 hidden classes and a particular optimization tier.","related":"chapter 71"},{"n":325,"category":"measurement-first performance","answer":"Performance work should identify whether the bottleneck is algorithmic, CPU, allocation/GC, DOM/rendering, network, I/O, or concurrency before optimizing.","wrong":"Micro-optimizing syntax is the first step in performance engineering.","related":"chapter 72"},{"n":329,"category":"source maps","answer":"Source maps map generated locations back to original source for debugging; they do not change runtime semantics and production exposure needs policy.","wrong":"Source maps make generated JavaScript execute as the original source.","related":"chapter 74"},{"n":333,"category":"feature detection","answer":"Standardization and implementation support are separate; use target policy/compat data and feature detection where appropriate rather than browser sniffing.","wrong":"If a feature is in ECMA-262 it is safe to use everywhere without checks.","related":"chapter 83"},{"n":337,"category":"async race conditions","answer":"Single-threaded callback execution does not prevent logical races: operations can interleave across awaits/tasks and stale results can overwrite newer state.","wrong":"Race conditions require two CPU threads executing JavaScript simultaneously.","related":"chapter 85"},{"n":341,"category":"error architecture","answer":"Application errors need contracts for expected/unexpected failures, cause/wrapping, retryability, cancellation, user messaging, logging and module boundaries.","wrong":"The best architecture catches every error at the lowest layer and returns null.","related":"chapter 86"},{"n":345,"category":"library public API","answer":"Stable libraries minimize accidental surface area, define errors/cancellation/side effects, document compatibility, and evolve with semver/deprecation strategy.","wrong":"A library's internal folder structure is automatically part of its supported API.","related":"chapter 88"},{"n":349,"category":"framework reactivity/scheduling","answer":"Framework-like systems separate dependency tracking, invalidation, scheduling/batching, rendering/effects and cleanup; Proxy is one tool, not the architecture itself.","wrong":"Using Proxy automatically provides a complete reactive framework.","related":"chapter 89"},{"n":353,"category":"compatibility governance","answer":"Staff-level JavaScript policy defines runtime/browser targets, dependency governance, security rules, performance budgets, migration strategy and shared-library ownership.","wrong":"Staff JavaScript architecture is mainly choosing a favorite framework.","related":"chapter 91"},{"n":357,"category":"ES2026 baseline","answer":"ECMAScript 2026 is ECMA-262 17th edition; its additions include Math.sumPrecise, Iterator.concat, Array.fromAsync, Error.isError, Map/WeakMap insertion helpers, Uint8Array base64/hex APIs, and JSON source/raw JSON capabilities.","wrong":"Temporal, decorators and explicit resource management are all ES2026 features.","related":"chapter 93"},{"n":361,"category":"TC39 stages","answer":"TC39 stages describe proposal maturity; Stage 4 is finished and can enter the living standard, while Stage 3 and below must not be presented as stable standard JavaScript.","wrong":"Stage 3 means the feature is standardized and production-stable.","related":"chapter 94"},{"n":365,"category":"Annex B","answer":"Annex B contains legacy web-compatibility semantics; understanding it explains old code but it should not be the default modern style.","wrong":"Annex B is a list of experimental future features.","related":"chapter 95"},{"n":369,"category":"spec reading","answer":"Read ECMA-262 by locating syntax/operation, following abstract operations and completion records, and distinguishing normative semantics, host hooks and implementation detail.","wrong":"The spec is only useful to engine implementers, not application engineers.","related":"chapter 96"},{"n":373,"category":"semantic case studies","answer":"Difficult behavior such as `[] == false`, detached methods, sparse arrays, property ordering, promise ordering and module cycles can be derived by tracing the relevant language rules.","wrong":"The only practical way to learn JavaScript edge cases is memorizing outputs.","related":"chapter 97"}];

export const levelFor = n => n <= 75 ? 'Beginner' : n <= 150 ? 'Intermediate' : n <= 225 ? 'Advanced' : n <= 300 ? 'Senior' : 'Staff / architecture';
export const variants = [
  {q: c => `Define ${c} precisely.`, reason:'Tests the governing mechanism.', follow:'Give one minimal example.', prefix:''},
  {q: c => `Debug a bug involving ${c}.`, reason:'Tests mechanism-first debugging.', follow:'What evidence would change your hypothesis?', prefix:'Apply this rule first: '},
  {q: c => `Name the misconception about ${c}.`, reason:'Tests guarantee vs folklore.', follow:'What bug can the misconception cause?', prefix:'Correct model: '},
  {q: c => `What production decision follows from ${c}?`, reason:'Tests turning semantics into contracts.', follow:'How would you regression-test it?', prefix:'Use this constraint: '},
];

export function Question({n, topic, variant}) {
  return <section>
    <h2>{`Q${String(n).padStart(3,'0')} — ${variant.q(topic.category)}`}</h2>
    <ul>
      <li><strong>Level:</strong> {levelFor(n)}</li>
      <li><strong>Category:</strong> {topic.category}</li>
      <li><strong>Expected answer:</strong> {variant.prefix}{topic.answer}</li>
      <li><strong>Reasoning:</strong> {variant.reason}</li>
      <li><strong>Common incorrect answer:</strong> {topic.wrong}</li>
      <li><strong>Follow-up:</strong> {variant.follow}</li>
      <li><strong>Related handbook chapter:</strong> {topic.related}</li>
    </ul>
  </section>;
}

{topics.flatMap(topic => variants.map((variant, offset) => (
  <Question key={topic.n + offset} n={topic.n + offset} topic={topic} variant={variant} />
)))}
