---
title: Q021–Q200
description: JavaScript interview questions 21–200 with answers, reasoning, misconceptions, follow-ups, and chapter mapping.
id: 02-q021-q200
---

# Q021–Q200
export const topics = [{"n":21,"category":"`typeof null`","answer":"`typeof null` is the historical string `\"object\"`; it is a legacy language behavior and does not mean null is an object.","wrong":"`null` is implemented as an object in ECMAScript.","related":"chapter 03"},{"n":25,"category":"truthiness","answer":"ToBoolean maps a fixed set of values to false (`false`, `0`, `-0`, `0n`, `NaN`, empty string, `null`, `undefined`); ordinary objects are truthy.","wrong":"Empty arrays and empty objects are falsy because they are empty.","related":"chapter 04"},{"n":29,"category":"ToPrimitive","answer":"Object-to-primitive conversion consults `Symbol.toPrimitive` first, then legacy methods in an order determined by the hint; operators may then convert the primitive further.","wrong":"Objects always call `toString()` before `valueOf()`.","related":"chapter 04"},{"n":33,"category":"loose equality","answer":"`==` follows Abstract Equality rules that may coerce types; odd cases should be derived from the algorithm rather than memorized.","wrong":"`==` is random and should never be reasoned about.","related":"chapter 05"},{"n":37,"category":"SameValueZero","answer":"SameValueZero treats `NaN` as equal to itself and treats `+0` and `-0` as equal; it is used by APIs such as `Set`, `Map` key matching, and `Array.prototype.includes`.","wrong":"Every JavaScript API uses `===` for equality.","related":"chapter 05"},{"n":41,"category":"short-circuiting","answer":"`&&`, `||`, `??`, optional chaining, and logical assignment conditionally skip evaluation; they return/assign based on value semantics rather than converting the final result to Boolean.","wrong":"Logical operators always return booleans.","related":"chapter 06"},{"n":45,"category":"optional chaining grouping","answer":"An optional chain short-circuits only along the continuous chain; grouping can end the chain so a later property access may still throw.","wrong":"Once `?.` appears anywhere in an expression the rest can never throw for nullish access.","related":"chapter 06"},{"n":49,"category":"`finally` completion","answer":"A `finally` block always executes after try/catch completion and an abrupt completion from `finally` can replace an earlier return or throw.","wrong":"`finally` can observe an error but cannot change the function result.","related":"chapter 07"},{"n":53,"category":"`for...in` vs `for...of`","answer":"`for...in` enumerates enumerable string property keys, including inherited ones; `for...of` consumes the iterable protocol and yields values.","wrong":"`for...in` is the normal way to iterate array values.","related":"chapter 08"},{"n":57,"category":"mutation during iteration","answer":"Iteration behavior depends on the specific algorithm and collection; mutating arrays/maps/sets during traversal can change what is visited and should not be generalized from one method.","wrong":"All iterators take a snapshot before iteration starts.","related":"chapter 08"},{"n":61,"category":"function declarations vs expressions","answer":"Function declarations create bindings during instantiation; expressions create function values when evaluated. Named function expressions also provide an inner self-name.","wrong":"Declarations and expressions differ only in style.","related":"chapter 09"},{"n":65,"category":"default parameters","answer":"Default parameter initializers run at call time in a parameter environment and only apply when the argument is `undefined`, not for other falsy values.","wrong":"Defaults run once when the function is defined.","related":"chapter 09"},{"n":69,"category":"closures","answer":"Closures preserve access to lexical bindings reachable from a function, which can keep associated values alive and observe later binding updates.","wrong":"A closure copies all outer variable values when the function is created.","related":"chapter 10"},{"n":73,"category":"execution context vs stack frame","answer":"Execution contexts are specification models for running code; physical stack frames are implementation details. They are related mental models but not identical guarantees.","wrong":"ECMA-262 requires a concrete stack frame for every function call.","related":"chapter 10"},{"n":77,"category":"ordinary-function `this`","answer":"For ordinary functions, `this` is determined by invocation form/receiver and strictness (plus explicit binding/constructor cases), not by lexical definition location.","wrong":"`this` always means the object where the function was written.","related":"chapter 11"},{"n":81,"category":"arrow `this`","answer":"Arrow functions do not define their own `this`; they capture the surrounding lexical `this` behavior and cannot be used as constructors.","wrong":"Arrow functions bind `this` to the object on their left.","related":"chapter 11"},{"n":85,"category":"`call`/`apply`/`bind`","answer":"`call` and `apply` invoke immediately with an explicit receiver; `bind` creates a new bound function with stored receiver/leading arguments and special constructor behavior.","wrong":"`bind` mutates the original function's `this`.","related":"chapter 12"},{"n":89,"category":"constructor invocation","answer":"`new F()` creates/chooses an object, sets prototype linkage from `F.prototype`, calls `F` with constructor semantics, and uses an explicitly returned object when one is returned.","wrong":"`new` just calls a function and always returns the newly allocated object.","related":"chapter 12"},{"n":93,"category":"property keys","answer":"Ordinary object property keys are strings or symbols; numeric-looking keys are converted to strings, while property ordering has defined categories rather than simple insertion order for every key.","wrong":"Object keys can be arbitrary object values like Map keys.","related":"chapter 13"},{"n":97,"category":"own vs inherited properties","answer":"Property lookup can traverse `[[Prototype]]`; ownership checks such as `Object.hasOwn` distinguish direct properties from inherited ones.","wrong":"If `key in obj` is true then the property is necessarily own.","related":"chapter 13"},{"n":101,"category":"property descriptors","answer":"Data descriptors use value/writable; accessor descriptors use get/set; enumerable and configurable affect visibility/redefinition/deletion. Descriptor defaults can be surprising in `defineProperty`.","wrong":"All object properties are writable, enumerable, and configurable by default regardless of how created.","related":"chapter 14"},{"n":105,"category":"freeze is shallow","answer":"`Object.freeze` prevents own-property reconfiguration/writes on that object but does not recursively freeze referenced objects.","wrong":"Freezing an object makes the entire reachable graph immutable.","related":"chapter 15"},{"n":109,"category":"prototype chain","answer":"Objects delegate missing-property lookup through `[[Prototype]]` until a property is found or `null` is reached.","wrong":"Prototype inheritance copies methods into each instance.","related":"chapter 16"},{"n":113,"category":"prototype pollution","answer":"Prototype pollution occurs when untrusted keys modify shared prototypes or prototype-bearing objects, affecting unrelated lookups; safe key handling and null-prototype/dedicated maps can reduce risk.","wrong":"Prototype pollution only matters if code explicitly calls `Object.setPrototypeOf`.","related":"chapter 16"},{"n":117,"category":"classes and prototypes","answer":"Class methods live on prototypes and inheritance uses the prototype system; classes add strict semantics, fields, private elements, `super`, and structured syntax.","wrong":"Classes replaced JavaScript's prototype model with classical inheritance.","related":"chapter 17"},{"n":121,"category":"private fields","answer":"`#private` elements use private names/brand checks and are not ordinary string-keyed properties accessible through reflection or bracket syntax.","wrong":"Private fields are normal properties whose names merely start with `#`.","related":"chapter 17"},{"n":125,"category":"sparse arrays","answer":"Array holes are absent indexed properties, not explicit `undefined`; methods differ in whether they skip holes, preserve them, or materialize values.","wrong":"A hole and an element containing `undefined` are always equivalent.","related":"chapter 18"},{"n":129,"category":"copy-by-change arrays","answer":"`toSorted`, `toReversed`, `toSpliced`, and `with` return changed copies instead of mutating the receiver, but the copies remain shallow.","wrong":"Non-mutating array methods deep-clone their elements.","related":"chapter 18"},{"n":133,"category":"Unicode strings","answer":"JavaScript strings are sequences of UTF-16 code units; iteration is code-point aware, while `.length` counts code units and user-perceived graphemes may require `Intl.Segmenter`.","wrong":"String length always equals the number of visible characters.","related":"chapter 19"},{"n":137,"category":"floating point","answer":"Number uses IEEE-754 binary64, so many decimal fractions are not exactly representable; `0.1 + 0.2` reflects representation/rounding rather than random arithmetic.","wrong":"JavaScript uses decimal floating point for ordinary Number values.","related":"chapter 20"},{"n":141,"category":"`NaN` and `-0`","answer":"`NaN` is unequal to itself under `===`; `Object.is` distinguishes `-0` from `+0` and treats NaN as same. Different APIs choose different equality relations.","wrong":"There is only one equality relation in JavaScript.","related":"chapter 20"},{"n":145,"category":"BigInt interoperability","answer":"BigInt supports integer arithmetic of arbitrary magnitude but cannot be mixed directly with Number arithmetic; comparison has specific interoperability rules and JSON needs an explicit strategy.","wrong":"BigInt is just a larger Number and can freely mix in arithmetic.","related":"chapter 21"},{"n":149,"category":"Date pitfalls","answer":"`Date` represents a timestamp while getters/parsing/formatting may involve local time or UTC; DST/time-zone assumptions make calendar arithmetic error-prone.","wrong":"A Date object stores the user's time zone together with the timestamp.","related":"chapter 22"},{"n":153,"category":"Temporal status","answer":"Temporal is Stage 4 in the post-ES2026 living work, but runtime/browser support must be checked; specification completion is not universal availability.","wrong":"Stage 4 means every supported browser already ships the feature.","related":"chapter 23"},{"n":157,"category":"RegExp backtracking","answer":"Backtracking regex engines can exhibit catastrophic runtime on ambiguous nested quantifiers; constrain patterns, inputs, and use safer formulations to mitigate ReDoS.","wrong":"Regular expressions always run in linear time.","related":"chapter 24"},{"n":161,"category":"symbols","answer":"Symbols are unique primitive property keys; the global registry via `Symbol.for` intentionally shares symbols by string key, while well-known symbols customize language protocols.","wrong":"Symbols are private properties that reflection can never see.","related":"chapter 25"},{"n":165,"category":"Map vs Object","answer":"Map accepts arbitrary key values, has defined insertion-order iteration, a size, and collection-specific APIs; objects are property bags with string/symbol keys and prototypes.","wrong":"Map is always faster than Object and should replace it everywhere.","related":"chapter 26"},{"n":169,"category":"weak collections","answer":"WeakMap/WeakSet do not keep their object/symbol keys alive solely through the collection and are intentionally non-enumerable because reachability is GC-sensitive.","wrong":"WeakMap automatically frees values on a deterministic schedule you can observe.","related":"chapter 26"},{"n":173,"category":"iterable protocol","answer":"An iterable has `Symbol.iterator` returning an iterator whose `next()` yields `{value, done}`; many syntactic forms consume this protocol and may perform iterator closing.","wrong":"Any object with a `next()` method is automatically iterable.","related":"chapter 27"},{"n":177,"category":"iterator helpers","answer":"Standard Iterator helpers create lazy transformations over iterator sequences; laziness changes allocation and timing, and iterator closing still matters.","wrong":"Iterator helpers eagerly create arrays like `map` on Array.","related":"chapter 27"},{"n":181,"category":"generators","answer":"Generator functions return generator objects and suspend/resume around `yield`; `next(value)`, `throw`, and `return` enable bidirectional control and cleanup.","wrong":"Calling a generator function immediately runs its body to completion.","related":"chapter 28"},{"n":185,"category":"typed arrays","answer":"TypedArrays are views over binary buffers with fixed element types; byte offsets/lengths and shared underlying buffers make aliasing important.","wrong":"TypedArrays are normal arrays that merely validate element types.","related":"chapter 29"},{"n":189,"category":"SharedArrayBuffer and Atomics","answer":"SharedArrayBuffer permits shared memory across agents; Atomics provides synchronization primitives. Shared memory introduces real data races and should be used only with a protocol.","wrong":"JavaScript cannot have data races because it is single-threaded.","related":"chapter 30"},{"n":193,"category":"Error cause","answer":"`Error` options can carry `cause` so layers can add context without discarding the original failure; stack traces are implementation/runtime features.","wrong":"Wrapping an error should replace and discard the original error.","related":"chapter 31"},{"n":197,"category":"Promise settlement","answer":"A Promise is pending then fulfilled or rejected; settlement is one-way, while resolution can adopt another thenable's eventual state.","wrong":"A resolved promise is necessarily already fulfilled.","related":"chapter 32"}];

export const levelFor = n => n <= 75 ? 'Beginner' : n <= 150 ? 'Intermediate' : n <= 225 ? 'Advanced' : n <= 300 ? 'Senior' : 'Staff / architecture';
export const variants = [
  {q: c => `Define ${c} precisely.`, reason:'Tests the governing mechanism.', follow:'Give one minimal example.', prefix:''},
  {q: c => `Debug a bug involving ${c}.`, reason:'Tests mechanism-first debugging.', follow:'What evidence would change your hypothesis?', prefix:'Apply this rule first: '},
  {q: c => `Name the misconception about ${c}.`, reason:'Tests guarantee vs folklore.', follow:'What bug can the misconception cause?', prefix:'Correct model: '},
  {q: c => `What production decision follows from ${c}?`, reason:'Tests turning semantics into contracts.', follow:'How would you regression-test it?', prefix:'Use this constraint: '},
];

export function Question({n, topic, variant}) {
  return <section>
    <h2>{`Q${String(n).padStart(3,'0')} — ${variant.q(topic.category)}`}</h2>
    <ul>
      <li><strong>Level:</strong> {levelFor(n)}</li>
      <li><strong>Category:</strong> {topic.category}</li>
      <li><strong>Expected answer:</strong> {variant.prefix}{topic.answer}</li>
      <li><strong>Reasoning:</strong> {variant.reason}</li>
      <li><strong>Common incorrect answer:</strong> {topic.wrong}</li>
      <li><strong>Follow-up:</strong> {variant.follow}</li>
      <li><strong>Related handbook chapter:</strong> {topic.related}</li>
    </ul>
  </section>;
}

{topics.flatMap(topic => variants.map((variant, offset) => (
  <Question key={topic.n + offset} n={topic.n + offset} topic={topic} variant={variant} />
)))}
