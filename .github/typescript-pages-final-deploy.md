# Temporary deployment trigger

This pull request exists only to trigger the validated default-branch Pages deployment workflow. It must be closed without merging after deployment verification.

Synchronization marker: workflow-run deployment chain enabled.
